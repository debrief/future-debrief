{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "outputs": ["dist/**"],
      "inputs": ["src/**", "schema/**", "derived/**"]
    },
    "dev": {
      "cache": false,
      "persistent": true,
      "inputs": ["src/**", "schema/**", "derived/**"]
    },
    "typecheck": {
      "outputs": []
    },
    "lint": {
      "outputs": []
    },
    "test": {
      "outputs": ["coverage/**"]
    },
    "storybook": {
      "cache": false,
      "persistent": true,
      "inputs": ["src/**", ".storybook/**", "stories/**"]
    }
  }
}