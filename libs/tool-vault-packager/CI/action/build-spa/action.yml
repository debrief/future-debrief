name: 'Build Tool Vault SPA'
description: 'Build the Single Page Application for Tool Vault'
inputs:
  ref:
    description: 'Git ref to checkout'
    required: false
    default: ''

outputs:
  spa_path:
    description: 'Path to the built SPA'
    value: 'libs/tool-vault-packager/spa/dist'

runs:
  using: 'composite'
  steps:
    - name: Build SPA using npm
      shell: bash
      working-directory: libs/tool-vault-packager
      run: |
        echo "🔗 Building SPA with npm..."
        echo "Working directory: $(pwd)"
        echo "Node version: $(node --version)"
        echo "NPM version: $(npm --version)"

        if [ ! -f "shared-types/src/index.ts" ] || [ ! -d "shared-types/src/types" ]; then
          echo "❌ shared-types artifacts missing in libs/tool-vault-packager/shared-types"
          ls -R shared-types || true
          exit 1
        fi

        # Build SPA (copies shared-types and builds automatically)
        npm run build:spa

        # Verify SPA was built
        if [ -d "spa/dist" ]; then
          echo "✅ SPA built successfully"
          ls -la spa/dist/
        else
          echo "❌ SPA build failed"
          exit 1
        fi
