name: 'Package Tool Vault PYZ'
description: 'Package the Tool Vault application into a .pyz file'
inputs:
  ref:
    description: 'Git ref to checkout'
    required: false
    default: ''

outputs:
  pyz_path:
    description: 'Path to the generated .pyz file'
    value: 'libs/tool-vault-packager/dist/toolvault.pyz'

runs:
  using: 'composite'
  steps:
    - name: Setup Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'

    - name: Clean up previous ToolVault comments
      shell: bash
      run: |
        # Delete previous ToolVault CI comments to avoid clutter
        if [ "$GITHUB_EVENT_NAME" = "pull_request" ]; then
          echo "🧹 Cleaning up previous ToolVault comments..."
          gh api repos/$GITHUB_REPOSITORY/issues/${{ github.event.pull_request.number }}/comments \
            --jq '.[] | select(.body | contains("## ToolVault Build")) | .id' | \
            xargs -I {} gh api repos/$GITHUB_REPOSITORY/issues/comments/{} -X DELETE || echo "No previous comments to delete"
        else
          echo "Not a pull request, skipping comment cleanup"
        fi
      env:
        GITHUB_TOKEN: ${{ github.token }}

    - name: Verify staged shared-types wheel
      shell: bash
      working-directory: libs/tool-vault-packager
      run: |
        # Verify that shared-types was staged by the main pipeline
        WHEEL_DIR="shared-types/dist/python"

        if [ ! -d "${WHEEL_DIR}" ]; then
          echo "Shared-types wheel directory not found at ${WHEEL_DIR}." >&2
          echo "Should have been staged by main pipeline." >&2
          exit 1
        fi

        WHEEL_PATH=$(find "${WHEEL_DIR}" -maxdepth 1 -name 'debrief_types-*.whl' -print -quit)

        if [ -z "${WHEEL_PATH}" ]; then
          echo "No debrief_types wheel found in ${WHEEL_DIR}." >&2
          exit 1
        fi

        echo "✅ Verified staged wheel: $(basename "${WHEEL_PATH}")"

    - name: Install Python dependencies
      shell: bash
      working-directory: libs/tool-vault-packager
      run: |
        pip install -r requirements.txt
    
    - name: Package PYZ
      shell: bash
      working-directory: libs/tool-vault-packager
      run: |
        npm run build

        # Verify PYZ was created
        if [ -f "dist/toolvault.pyz" ]; then
          echo "✅ PYZ packaged successfully"
          ls -la dist/toolvault.pyz
          echo "PYZ file size: $(du -h dist/toolvault.pyz | cut -f1)"
        else
          echo "❌ PYZ packaging failed"
          exit 1
        fi
