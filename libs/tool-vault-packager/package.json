{
  "name": "tool_vault_packager",
  "version": "1.0.0",
  "description": "ToolVault packaging system for creating deployable .pyz files",
  "main": "cli.py",
  "scripts": {
    "clean": "rm -rf dist && rm -rf tmp_package_contents",
    "build": "mkdir -p dist && python packager.py tools --output dist/toolvault.pyz && mkdir -p ../../apps/vs-code/tool-vault && cp dist/toolvault.pyz ../../apps/vs-code/tool-vault/",
    "build:spa": "cd spa && npm install && npm run verify-deps && npm run build",
    "prebuild": "npm run clean && npm run build:spa",
    "serve": "python cli.py serve",
    "dev:spa": "cd spa && npm run dev",
    "dev:spa:with-backend": "cd spa && npm run dev:with-backend",
    "test": "test -f dist/toolvault.pyz && echo 'Test passed: dist/toolvault.pyz exists' || (echo 'Test failed: dist/toolvault.pyz not found' && exit 1)",
    "test:tools": "python -m testing.cli test",
    "test:tools:baseline": "python -m testing.cli generate-baseline",
    "test:tools:report": "python -m testing.cli test --save-report test_report.json",
    "test:playwright": "playwright test",
    "test:playwright:ui": "playwright test --ui",
    "test:playwright:debug": "playwright test --debug",
    "test:playwright:both": "node test-runner.js",
    "test:playwright:dev": "node test-runner.js dev",
    "test:playwright:pyz": "node test-runner.js pyz",
    "validate": "make verify",
    "verify:shared-types": "test -f shared-types/src/index.ts || (echo '❌ shared-types structure invalid - run: cd ../shared-types && make copy-to-tool-vault-packager' && exit 1)",
    "install:dev-deps": "make install-dev-deps",
    "lint": "make lint",
    "lint:fix": "make lint-fix",
    "typecheck": "make type-check",
    "check:all": "make check-all"
  },
  "keywords": [
    "toolvault",
    "mcp",
    "packaging"
  ],
  "author": "ToolVault",
  "license": "MIT",
  "dependencies": {
    "@debrief/shared-types": "file:./shared-types"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "typescript": "^5.0.0"
  }
}
