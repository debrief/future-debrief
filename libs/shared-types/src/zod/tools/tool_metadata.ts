import { z } from "zod"

export default z.object({ "name": z.string().describe("The unique identifier/name of the tool"), "description": z.string().describe("Human-readable description of what the tool does"), "parameters": z.record(z.any()).describe("Typed parameter schema for the tool"), "return_type": z.string().describe("Return type annotation as string"), "module_path": z.string().describe("Path to the tool's module file"), "tool_dir": z.string().describe("Directory containing the tool"), "sample_inputs": z.array(z.any()).describe("Sample input data for testing").optional(), "source_code": z.union([z.string(), z.null()]).describe("Tool's source code").default(null), "git_history": z.array(z.any()).describe("Git commit history for this tool").optional() }).strict().describe("Enhanced ToolMetadata class with typed parameters, sample_inputs, git_history.")
